name: Portfolio CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  quality-checks:
    runs-on: ubuntu-latest
    name: 🔍 Quality Checks

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🧹 Clean npm cache and dependencies
        run: |
          rm -rf node_modules
          rm -f package-lock.json
          npm cache clean --force

      - name: 📋 Install dependencies
        run: npm install

      - name: 🔍 Run ESLint
        run: npm run lint

      - name: 🎨 Check Prettier formatting
        run: npm run format:check

      - name: 🧪 Run tests
        run: npm run test:run

      - name: 📊 Run tests with coverage
        run: npm run test:coverage

      - name: 🔒 Run security checks
        run: npm run security:check

      - name: 🏗️ Test build
        run: npm run build

      - name: 📈 Upload test coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage/
          retention-days: 30

  deploy:
    needs: quality-checks
    runs-on: ubuntu-latest
    name: 🚀 Deploy to Cloudflare
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🧹 Clean npm cache and dependencies
        run: |
          rm -rf node_modules
          rm -f package-lock.json
          npm cache clean --force

      - name: 📋 Install dependencies
        run: npm install

      - name: 🏗️ Build React portfolio
        run: npm run build

      - name: 🧹 Remove _redirects file (if exists)
        run: rm -f dist/_redirects

      - name: 🔐 Set Cloudflare secrets for production
        run: |
          echo "Setting Cloudflare secrets for production deployment..."

          # Set Telegram Bot Token
          if [ -n "${{ secrets.TELEGRAM_BOT_TOKEN }}" ]; then
            echo "${{ secrets.TELEGRAM_BOT_TOKEN }}" | npx wrangler secret put TELEGRAM_BOT_TOKEN --env production
            echo "✅ TELEGRAM_BOT_TOKEN set for production"
          else
            echo "⚠️  TELEGRAM_BOT_TOKEN not found in GitHub secrets"
          fi

          # Set Telegram Admin Chat ID
          if [ -n "${{ secrets.TELEGRAM_ADMIN_CHAT_ID }}" ]; then
            echo "${{ secrets.TELEGRAM_ADMIN_CHAT_ID }}" | npx wrangler secret put TELEGRAM_ADMIN_CHAT_ID --env production
            echo "✅ TELEGRAM_ADMIN_CHAT_ID set for production"
          else
            echo "⚠️  TELEGRAM_ADMIN_CHAT_ID not found in GitHub secrets"
          fi

          # Set SMTP User
          if [ -n "${{ secrets.SMTP_USER }}" ]; then
            echo "${{ secrets.SMTP_USER }}" | npx wrangler secret put SMTP_USER --env production
            echo "✅ SMTP_USER set for production"
          else
            echo "⚠️  SMTP_USER not found in GitHub secrets"
          fi

          # Set SMTP Password
          if [ -n "${{ secrets.SMTP_PASS }}" ]; then
            echo "${{ secrets.SMTP_PASS }}" | npx wrangler secret put SMTP_PASS --env production
            echo "✅ SMTP_PASS set for production"
          else
            echo "⚠️  SMTP_PASS not found in GitHub secrets"
          fi

          # Set SMTP Host
          if [ -n "${{ secrets.SMTP_HOST }}" ]; then
            echo "${{ secrets.SMTP_HOST }}" | npx wrangler secret put SMTP_HOST --env production
            echo "✅ SMTP_HOST set for production"
          else
            echo "⚠️  SMTP_HOST not found in GitHub secrets"
          fi

          # Set Feedback Email
          if [ -n "${{ secrets.FEEDBACK_EMAIL }}" ]; then
            echo "${{ secrets.FEEDBACK_EMAIL }}" | npx wrangler secret put FEEDBACK_EMAIL --env production
            echo "✅ FEEDBACK_EMAIL set for production"
          else
            echo "⚠️  FEEDBACK_EMAIL not found in GitHub secrets"
          fi

          # Set Contact Email
          if [ -n "${{ secrets.CONTACT_EMAIL }}" ]; then
            echo "${{ secrets.CONTACT_EMAIL }}" | npx wrangler secret put CONTACT_EMAIL --env production
            echo "✅ CONTACT_EMAIL set for production"
          else
            echo "⚠️  CONTACT_EMAIL not found in GitHub secrets"
          fi

          echo "🔐 All secrets configured for production deployment"
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: 🚀 Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          wranglerVersion: 'latest'
          workingDirectory: '.'
          command: deploy --env production --compatibility-date=2024-01-01

      - name: 🎉 Deployment notification
        if: success()
        run: |
          echo "🎉 Portfolio successfully deployed to https://www.aswinlocal.in/"
          echo "✅ Deployment completed at $(date)"

  pr-comment:
    needs: quality-checks
    runs-on: ubuntu-latest
    name: 💬 PR Status Comment
    if: github.event_name == 'pull_request'

    steps:
      - name: 📝 Comment on PR
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## ✅ Quality Checks Passed!
              
              🔍 **Linting**: ✅ Passed  
              🎨 **Formatting**: ✅ Passed  
              🧪 **Tests**: ✅ Passed  
              🔒 **Security**: ✅ Passed  
              🏗️ **Build**: ✅ Passed  
              
              Ready for review and merge! 🚀`
            })
